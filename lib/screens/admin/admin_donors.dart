import 'package:flutter/material.dart';

class ViewDonors extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'View All Donors',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
