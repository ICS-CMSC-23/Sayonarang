import 'package:flutter/material.dart';

class AdminApprovalWaitList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'List of organization that signed up',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
